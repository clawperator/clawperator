interface:
  display_name: "Task Cleanup"
  short_description: "Retire completed task packs safely"
  default_prompt: "Use $task-cleanup to close out a completed task pack. Read SKILL.md first, then run all three gates in order: migrate durable knowledge, remove stale phase references from code, verify plan fidelity. Only delete the task folder after all gates pass."
