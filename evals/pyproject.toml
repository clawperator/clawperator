[project]
name = "clawperator-evals"
version = "0.0.0"
description = "Portable eval harness for Clawperator"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
