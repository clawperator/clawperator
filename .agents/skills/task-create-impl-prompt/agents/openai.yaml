interface:
  display_name: "Task Impl Prompt"
  short_description: "Draft one-PR implementation prompts"
  default_prompt: "Use $task-create-impl-prompt to draft a concise implementation prompt for PR-1 of a task pack, with a hard stop before later PRs."
