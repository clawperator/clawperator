# PR-1 Implementation Prompt

Implement `tasks/api/selector-inspection` PR-1 (R4): structured node queries and shared relational selector resolution. Scope is Phase 1 in [work-breakdown.md](work-breakdown.md), with [plan.md](plan.md) as the contract and source pointers.

Prerequisite: No additional feature dependency. Verify any required merges in your implementation branch before dependent work.

Use the Android tree/resolver as the authority and expose the query through Node/CLI/MCP; preserve empty-label state and observation-local identity.

Complete this PR through implementation, the specified regression and applicable live evidence, in-scope repairs, docs/status updates, and narrow local commits. Make routine implementation choices within the contract; continue until acceptance is satisfied or an external blocker leaves specific evidence unproven.

Do not implement or scaffold PR-2 strict action integration; it needs a separate handoff after PR-1 merges. Finish with commits and a concise account of validation and any remaining blocker. This prompt does not authorize pushing, merging, or release publication.
